# agents/

Sub-agent definitions shipped with the agent-lock plugin.

Currently empty. Reserved for a future `agent-lock-arbiter` agent that
mediates force-release decisions when a peer's lock is contested.
